# 微信云托管（CloudBase Run）部署指南 —— 让朋友随时测你的旅行小程序

> 为什么选云托管而不是普通云函数？
> 你的真实行程生成要 **~112 秒**，而普通云函数单次调用最长 **60 秒**会被掐断。
> 云托管是**常驻容器**，能跑我们这套「提交任务 + 轮询」的异步模式；
> 而且它的**默认域名微信自动放行白名单**，朋友扫体验版**不用备案、不用配域名**。

---

## 整体流程一览

```
开发者工具开通云开发
   └─ 云开发控制台 → 云托管 → 新建服务
        └─ 上传 server/ 代码（GitHub 关联 或 本地 zip）
             └─ 自动用 Dockerfile 构建镜像
                  └─ 拿到默认域名 https://xxxx.run.tcloudbase.com
                       └─ 改 app.js 的 apiBase = 该域名
                            └─ 开发者工具「上传」→ 体验版
                                 └─ 后台加朋友为「体验成员」→ 朋友扫码测
```

---

## 第 1 步：开通云开发

1. 打开微信开发者工具，确保已导入 `miniprogram/` 项目。
2. 点顶部工具栏的 **「云开发」** 按钮（云朵图标）。
3. 首次会让你**开通**：选一个环境（如 `travel-prod`），点确认 → 自动创建一个 CloudBase 环境，**记下环境 ID**（形如 `travel-prod-xxxxxx`）。
4. 开通后弹出「云开发控制台」窗口。

> 个人主体也能开通云开发，免费额度够测试用。

---

## 第 2 步：新建云托管服务

1. 在云开发控制台左侧点 **「云托管」**。
2. 点 **「新建服务」**：
   - 服务名称：`travel-plan`（随意）
   - 服务备注：可空
   - 点击创建
3. 进入该服务，**「代码部署」** 标签页 → **「新建版本」**。

---

## 第 3 步：上传后端代码（二选一）

### 方式 A：本地打包 zip 上传（最简单，不用 GitHub）

1. 把 `server/` 整个文件夹压缩成 **`server.zip`**（确保 zip 根目录直接是 `main.py`、`Dockerfile`、`requirements.txt` 等，不要多套一层文件夹）。
2. 在「新建版本」里选 **「本地代码」/「上传代码包」**，上传 `server.zip`。
3. 构建方式选 **「Dockerfile」**（会自动读我们的 `Dockerfile`）。

### 方式 B：关联 GitHub 自动构建（推荐，以后改代码点一下就重新部署）

1. 「新建版本」里选 **「代码托管（GitHub）」** → 授权微信访问你的 GitHub。
2. 仓库选你放的 `server/` 目录所在仓库（建议把 `server/` 单独推到一个仓库，如 `travel-agent-server`）。
3. 分支：`main`（或你用的分支）。
4. 代码目录：填 `server`（容器构建的上下文根，里面要有 Dockerfile）。
5. 构建方式：**「Dockerfile」**。

---

## 第 4 步：构建配置（关键）

在「新建版本」表单里填：

| 配置项 | 填什么 | 说明 |
|--------|--------|------|
| **监听端口** | `80` | 必须和 `Dockerfile` 里 `EXPOSE 80` / uvicorn `--port 80` 一致 |
| **环境变量** | `DEEPSEEK_API_KEY` = `sk-你的key` | 别写进代码/.env，云托管控制台配最安全 |
| **健康检查路径** | `/health` | 部署后会自动探活，通了才算就绪 |
| **最小副本数** | `0` 或 `1` | 测试选 `0`（省钱，闲置缩容）；想随时响应选 `1` |
| **超时时间** | `300` 秒 | 给真实生成留足；云托管支持到 300s+ |

> 你的 `server/.env` 里有 Key，但 **`.dockerignore` 已把它排除出镜像**，所以必须在这里用环境变量再配一次，否则容器里读不到 Key 会走 mock 模式。

点 **「开始部署」**，等几分钟（首次要拉 python 镜像 + 装依赖，慢点正常）。

---

## 第 5 步：拿到默认域名

部署成功后，在「云托管 → 服务详情」能看到一个 **默认域名**，形如：

```
https://travel-plan-xxxxxx.ap-shanghai.run.tcloudbase.com
```

复制它（含 `https://`）。**这个域名微信已默认加入 request 合法域名白名单**，体验版/正式版都不用再去后台配。

---

## 第 6 步：改前端地址

打开 `miniprogram/app.js`，把 `apiBase` 改成刚复制的域名：

```js
apiBase: "https://travel-plan-xxxxxx.ap-shanghai.run.tcloudbase.com"
```

保存 → 开发者工具 **「编译」**。

> 此时「不校验合法域名」勾不勾都行——云托管域名已被放行。

---

## 第 7 步：自己先测一遍

1. 开发者工具模拟器里填目的地/天数 → 点「生成行程」。
2. 加载页转几秒 → 应出现**行程卡片 + 真实天气**（证明云托管后端通了，而且能跑真实 112s 生成）。
3. 若失败，看弹窗里的真实错误；常见：
   - `request:fail` + 域名相关 → apiBase 没改对 / 少了 `https://`
   - `mock_mode` 相关 / 行程是示例 → `DEEPSEEK_API_KEY` 环境变量没配
   - 超时 → 检查云托管「超时时间」是否 ≥ 300s、副本是否就绪

---

## 第 8 步：上传体验版 + 加朋友

1. 开发者工具点 **「上传」**（不是预览）→ 填版本号如 `1.0.0` → 提交。
2. 登录小程序后台 <https://mp.weixin.qq.com> → **「管理 → 成员管理 → 体验者」** → 添加朋友的**微信号**。
3. 在「云托管」或小程序后台的「版本管理 → 体验版」拿到**体验版二维码**，发给朋友扫。
4. 朋友微信扫 → 进入体验版 → 填目的地 → 生成 → 就能用上和你一样的 **DeepSeek + 联网搜索** 真实行程。

> 体验版最多可加几十个体验成员，足够朋友测试。正式发布给用户才需要走备案 + 审核。

---

## 常见问题

**Q：云托管要备案吗？**
A：用默认域名（`*.run.tcloudbase.com`）测试**不用备案**。只有你想换成自己的自定义域名（如 `api.your-site.com`）时才需 ICP 备案。

**Q：朋友在自己流量下能打开吗？**
A：能。云托管是公网 HTTPS 服务，朋友手机在哪都能连，不要求同 WiFi（这点和本地路线 A 不同）。

**Q：要一直开电脑吗？**
A：不用。云托管是腾讯云的常驻容器，你关电脑朋友照样能测。

**Q：多少钱？**
A：新人有一定免费额度；超出按量计费。测试阶段基本白嫖。上线前建议在云托管设个调用频率限制，避免朋友猛点把 DeepSeek 额度刷爆。

**Q：改了后端代码怎么更新？**
A：方式 A 重新传 zip；方式 B 在 GitHub 推新 commit 后，云托管控制台点「重新部署」即可。
